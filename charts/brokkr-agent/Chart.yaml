apiVersion: v2
name: brokkr-agent
description: Brokkr Kubernetes cluster agent
type: application
version: 0.8.3
appVersion: "0.8.3"
# Shipwright integration (when enabled) requires Kubernetes >= 1.29
kubeVersion: ">=1.29.0-0"

# Note: Tekton and Shipwright are installed via pre-install hook Jobs
# using official manifests. See values.yaml shipwright.install settings.
