---
id: r2-b-rename-python-sdk
level: task
title: "R2-B: Rename Python SDK distributions to brokkr-client / brokkr-client-generated"
short_code: "BROKKR-T-0146"
created_at: 2026-05-15T22:30:00+00:00
updated_at: 2026-05-15T23:39:28.856428+00:00
parent: BROKKR-I-0018
blocked_by: []
archived: true

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: BROKKR-I-0018
---

# R2-B: Rename Python SDK distributions to brokkr-client / brokkr-client-generated

## Parent Initiative

[[BROKKR-I-0018]]

## Objective

Bring the two Python packages' **distribution names** in line with what's reserved on PyPI: wrapper goes `brokkr` → `brokkr-client`, generated low-level goes `brokkr-broker-client` → `brokkr-client-generated`. Import names stay the same to avoid a churning refactor for zero ergonomic gain.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [x] `sdks/python/brokkr/pyproject.toml` has `[project] name = "brokkr-client"`.
- [x] `sdks/python/brokkr-client/pyproject.toml` has `[project] name = "brokkr-client-generated"`.
- [x] `sdks/python/brokkr/pyproject.toml`'s `dependencies` and `tool.uv.sources` reference `brokkr-client-generated`, not `brokkr-broker-client`.
- [x] Import names unchanged: `from brokkr import BrokkrClient` and `from brokkr_broker_client import ...` continue to work.
- [x] `angreal openapi gen-python` produces output with the renamed distribution via `--config .angreal/files/openapi-python-client-config.yaml` (`project_name_override: brokkr-client-generated`, `package_name_override: brokkr_broker_client`).
- [x] `uv pip install -e sdks/python/brokkr` succeeds against the renamed packages in a clean venv (verified; 24/24 wrapper tests pass).
- [x] `angreal openapi check-python` passes against the renamed sources.
- [x] `docs/src/how-to/sdks/python.md` updated to reference new names. (Install command kept as path-dep style; switches to `pip install brokkr-client` in R2-F after first publish.)
- [x] Other docs swept: `docs/src/how-to/sdks/README.md`, `sdks/python/brokkr/README.md`, `sdks/python/brokkr-client/README.md` (latter regenerated by the new config).

## Implementation Notes

### Technical Approach

1. Rename in `pyproject.toml`s first. Keep the directory layout (`sdks/python/brokkr/` and `sdks/python/brokkr-client/`) — directory names are independent of distribution names and renaming them is unnecessary churn.
2. Update `tool.uv.sources` in the wrapper's `pyproject.toml` to point at the renamed generated package by its new distribution name.
3. `openapi-python-client` config (likely a `pyproject.toml` block or CLI flag in the angreal task) sets the package's distribution name on regeneration — update it so future `gen-python` runs produce the right name.
4. Sweep docs. The two install-line locations are `docs/src/how-to/sdks/python.md` and the SDK READMEs in `sdks/python/*/README.md`.

### Dependencies

- None (can run in parallel with R2-A).

### Risk Considerations

- The generator-emitted package name has to be settable. If `openapi-python-client` insists on a name derived from the OpenAPI `info.title`, we may need a post-generation `sed` step or a config file. Verify before assuming a flag exists.
- Import name `brokkr_broker_client` stays. This is awkward (mismatched distribution and import names) but the alternative — renaming every `from brokkr_broker_client...` site — is a larger refactor for purely cosmetic improvement.

## Status Updates

- 2026-05-15: Implemented.
  - Added `.angreal/files/openapi-python-client-config.yaml` with `project_name_override: brokkr-client-generated` and `package_name_override: brokkr_broker_client` (keeps import name unchanged). Wired the `--config` flag into `_run_python_gen()` so every regeneration respects the override.
  - Renamed wrapper distribution: `sdks/python/brokkr/pyproject.toml` `name` → `brokkr-client`; dep + `tool.uv.sources` switched to `brokkr-client-generated`. Import name (`brokkr`) unchanged.
  - Ran `angreal openapi gen-python`: regenerated `sdks/python/brokkr-client/pyproject.toml` with the new distribution name; module-name stays `brokkr_broker_client`. README's H1 was regenerated to match.
  - Verified all three drift checks pass post-rename: `angreal openapi check`, `check-python`, `check-typescript`.
  - Clean-venv install: `uv pip install -e sdks/python/brokkr[test]` resolves `brokkr-client==0.0.0` and `brokkr-client-generated==0.0.0`; all 24 wrapper tests pass; imports `from brokkr import BrokkrClient` and `from brokkr_broker_client import AuthenticatedClient` both work.
  - Docs swept: `docs/src/how-to/sdks/README.md`, `python.md`, `sdks/python/brokkr/README.md`. Final install-command flip to `pip install brokkr-client` deferred to R2-F (needs the package actually on PyPI first).